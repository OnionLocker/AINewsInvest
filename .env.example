# .env.example - 复制为 .env 并填写真实值
# cp .env.example .env

# ── Flask 会话密钥（首次 deploy.sh 自动生成）──────────────────────
# SECRET_KEY=

# ── Fernet 对称加密密钥（用于加密用户的 TG Token 等敏感数据）─────
# 首次部署时 deploy.sh 自动生成并写入
# ENCRYPT_KEY=
